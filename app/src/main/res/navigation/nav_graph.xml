<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/loginFragment">
    <navigation
        android:id="@+id/loginNavigation"
        app:startDestination="@id/statusFragment">
        <fragment
            android:id="@+id/statusFragment"
            android:name="com.example.applover.ui.login.StatusFragment"
            android:label="StatusFragment"
            tools:layout="@layout/status_fragment" />
        <fragment
            android:id="@+id/loginFragment"
            android:name="com.example.applover.ui.login.LoginFragment"
            android:label="LoginFragment"
            tools:layout="@layout/login_fragment">
            <action
                android:id="@+id/action_loginFragment2_to_statusFragment"
                app:destination="@id/statusFragment" />
        </fragment>
    </navigation>
</navigation>